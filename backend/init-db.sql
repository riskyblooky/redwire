-- Initialize database with extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- This file is executed only once when the database is first created
-- Alembic migrations will handle the actual schema creation
